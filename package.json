{
  "name": "streambox",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-config.js",
    "start": "npm start --prefix server",
    "dev": "node scripts/build-config.js && npm start --prefix server",
    "install:all": "npm install --prefix server"
  }
}
